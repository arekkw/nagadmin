import {
  resizeImage
} from 'nag-admin/helpers/resize-image';

module('ResizeImageHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = resizeImage(42);
  ok(result);
});
